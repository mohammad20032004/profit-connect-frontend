// API endpoints constants
export const API_ENDPOINTS = {
  BASE_URL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000/api',
  // Intentionally empty: endpoints will be wired manually later.
};
